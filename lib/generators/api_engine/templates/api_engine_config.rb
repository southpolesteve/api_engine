ApiEngine.configure do |config|

  # Whitelist is a way to restrict API access to only a subset of models.
  # by default this is set to 'false' and all models are exposed via the
  # default API
  #
  # config.whitelist = [:post, :comment, :user]

end